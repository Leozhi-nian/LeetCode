package com.leozhi.topic50

import com.leozhi.testcase.array.IntegerArray

/**
 * @author leozhi
 * 50. Pow(x, n)
 * 实现 pow(x, n) ，即计算 x 的 n 次幂函数（即，xn）。
 */
fun main() {
    Solution01().myPow(2.0, -3)
}